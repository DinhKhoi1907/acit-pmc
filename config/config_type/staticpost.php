<?php

    $config = array();


    /* Footer */

    $nametype = "vechungtoi";

    $config[$nametype]['com'] = 've-chung-toi';

    $config[$nametype]['menu'] = true;

    $config[$nametype]['title_main'] = "Giới thiệu";

    $config[$nametype]['images'] = true;

    $config[$nametype]['images2'] = false;

    $config[$nametype]['file'] = false;

    $config[$nametype]['tieude'] = true;

    $config[$nametype]['mota'] = true;

    $config[$nametype]['mota_cke'] = true;

    $config[$nametype]['noidung'] = true;

    $config[$nametype]['noidung_cke'] = true;

    $config[$nametype]['mota_other'] = false;

    $config[$nametype]['slogan'] = false;

    $config[$nametype]['giatri'] = false;

    $config[$nametype]['video'] = false;

    $config[$nametype]['seo'] = true;

    $config[$nametype]['gallery'] = false;

    $config[$nametype]['width'] = 600;

    $config[$nametype]['height'] = 600;

    $config[$nametype]['width2'] = 374;

    $config[$nametype]['height2'] = 549;

    $config[$nametype]['banner'] = true;

    $config[$nametype]['width_banner'] = 1920;

    $config[$nametype]['height_banner'] = 450;

    $config[$nametype]['width_poster'] = 514;

    $config[$nametype]['height_poster'] = 329;

    $config[$nametype]['width_photoother'] = 480;

    $config[$nametype]['height_photoother'] = 480;

    $config[$nametype]['width_gallery'] = 480;

    $config[$nametype]['height_gallery'] = 480;

    $config[$nametype]['ratio'] = 1;

    $config[$nametype]['amount_images'] = 1;

    $config[$nametype]['img_type'] = '.jpg|.gif|.png|.jpeg|.gif|.JPG|.PNG|.JPEG|.Png|.GIF';

    $config[$nametype]['img_type_photo'] = '.jpg|.gif|.png|.jpeg|.gif|.JPG|.PNG|.JPEG|.Png|.GIF';

    $config[$nametype]['file_type'] = 'doc|docx|pdf|rar|zip|ppt|pptx|DOC|DOCX|PDF|RAR|ZIP|PPT|PPTX|xls|jpg|png|gif|JPG|PNG|GIF|xls|XLS';
    $config[$nametype]['gallery'] = array
    (
        $nametype => array
        (
            "title_main_photo" => "Hình ảnh sản phẩm",
            "title_sub_photo" => "Hình ảnh",
            "number_photo" => 3,
            "images_photo" => true,
            "cart_photo" => false,
            "avatar_photo" => true,
            "tieude_photo" => true,
            "width_photo" => 800,
            "height_photo" => 800,
            "thumb_photo" => '800x800x1',
            "img_type_photo" => '.jpg|.gif|.png|.jpeg|.gif|.JPG|.PNG|.JPEG|.Png|.GIF'
        )
    );


    /* Footer */

    // $nametype = "vision";

    // $config[$nametype]['com'] = 'vision';

    // $config[$nametype]['menu'] = true;

    // $config[$nametype]['title_main'] = "Vision";

    // $config[$nametype]['images'] = true;

    // $config[$nametype]['images2'] = false;

    // $config[$nametype]['file'] = false;

    // $config[$nametype]['tieude'] = false;

    // $config[$nametype]['mota'] = false;

    // $config[$nametype]['mota_cke'] = false;

    // $config[$nametype]['noidung'] = true;

    // $config[$nametype]['noidung_cke'] = true;

    // $config[$nametype]['mota_other'] = false;

    // $config[$nametype]['slogan'] = false;

    // $config[$nametype]['giatri'] = false;

    // $config[$nametype]['video'] = false;

    // $config[$nametype]['seo'] = false;

    // $config[$nametype]['gallery'] = false;

    // $config[$nametype]['width'] = 575;

    // $config[$nametype]['height'] = 442;

    // $config[$nametype]['width2'] = 374;

    // $config[$nametype]['height2'] = 549;

    // $config[$nametype]['banner'] = false;

    // $config[$nametype]['width_banner'] = 1440;

    // $config[$nametype]['height_banner'] = 280;

    // $config[$nametype]['width_poster'] = 514;

    // $config[$nametype]['height_poster'] = 329;

    // $config[$nametype]['width_photoother'] = 480;

    // $config[$nametype]['height_photoother'] = 480;

    // $config[$nametype]['width_gallery'] = 480;

    // $config[$nametype]['height_gallery'] = 480;

    // $config[$nametype]['ratio'] = 1;

    // $config[$nametype]['amount_images'] = 1;

    // $config[$nametype]['img_type'] = '.jpg|.gif|.png|.jpeg|.gif|.JPG|.PNG|.JPEG|.Png|.GIF';

    // $config[$nametype]['img_type_photo'] = '.jpg|.gif|.png|.jpeg|.gif|.JPG|.PNG|.JPEG|.Png|.GIF';

    // $config[$nametype]['file_type'] = 'doc|docx|pdf|rar|zip|ppt|pptx|DOC|DOCX|PDF|RAR|ZIP|PPT|PPTX|xls|jpg|png|gif|JPG|PNG|GIF|xls|XLS';


    /* Footer */

    // $nametype = "mission";

    // $config[$nametype]['com'] = 'mission';

    // $config[$nametype]['menu'] = false;

    // $config[$nametype]['title_main'] = "Mission";

    // $config[$nametype]['images'] = true;

    // $config[$nametype]['images2'] = false;

    // $config[$nametype]['file'] = false;

    // $config[$nametype]['tieude'] = false;

    // $config[$nametype]['mota'] = false;

    // $config[$nametype]['mota_cke'] = false;

    // $config[$nametype]['noidung'] = true;

    // $config[$nametype]['noidung_cke'] = true;

    // $config[$nametype]['mota_other'] = false;

    // $config[$nametype]['slogan'] = false;

    // $config[$nametype]['giatri'] = false;

    // $config[$nametype]['video'] = false;

    // $config[$nametype]['seo'] = false;

    // $config[$nametype]['gallery'] = false;

    // $config[$nametype]['width'] = 575;

    // $config[$nametype]['height'] = 442;

    // $config[$nametype]['width2'] = 374;

    // $config[$nametype]['height2'] = 549;

    // $config[$nametype]['banner'] = false;

    // $config[$nametype]['width_banner'] = 1440;

    // $config[$nametype]['height_banner'] = 280;

    // $config[$nametype]['width_poster'] = 514;

    // $config[$nametype]['height_poster'] = 329;

    // $config[$nametype]['width_photoother'] = 480;

    // $config[$nametype]['height_photoother'] = 480;

    // $config[$nametype]['width_gallery'] = 480;

    // $config[$nametype]['height_gallery'] = 480;

    // $config[$nametype]['ratio'] = 1;

    // $config[$nametype]['amount_images'] = 1;

    // $config[$nametype]['img_type'] = '.jpg|.gif|.png|.jpeg|.gif|.JPG|.PNG|.JPEG|.Png|.GIF';

    // $config[$nametype]['img_type_photo'] = '.jpg|.gif|.png|.jpeg|.gif|.JPG|.PNG|.JPEG|.Png|.GIF';

    // $config[$nametype]['file_type'] = 'doc|docx|pdf|rar|zip|ppt|pptx|DOC|DOCX|PDF|RAR|ZIP|PPT|PPTX|xls|jpg|png|gif|JPG|PNG|GIF|xls|XLS';


    /* Footer */

    // $nametype = "catalogue";

    // $config[$nametype]['com'] = 'catalogue';

    // $config[$nametype]['menu'] = false;

    // $config[$nametype]['title_main'] = "Catalogue";

    // $config[$nametype]['images'] = false;

    // $config[$nametype]['images2'] = false;

    // $config[$nametype]['file'] = true;

    // $config[$nametype]['tieude'] = false;

    // $config[$nametype]['mota'] = false;

    // $config[$nametype]['mota_cke'] = false;

    // $config[$nametype]['noidung'] = false;

    // $config[$nametype]['noidung_cke'] = false;

    // $config[$nametype]['mota_other'] = false;

    // $config[$nametype]['slogan'] = false;

    // $config[$nametype]['giatri'] = false;

    // $config[$nametype]['video'] = false;

    // $config[$nametype]['seo'] = true;

    // $config[$nametype]['gallery'] = false;

    // $config[$nametype]['width'] = 517;

    // $config[$nametype]['height'] = 502;

    // $config[$nametype]['width2'] = 374;

    // $config[$nametype]['height2'] = 549;

    // $config[$nametype]['banner'] = false;

    // $config[$nametype]['width_banner'] = 1440;

    // $config[$nametype]['height_banner'] = 280;

    // $config[$nametype]['width_poster'] = 514;

    // $config[$nametype]['height_poster'] = 329;

    // $config[$nametype]['width_photoother'] = 480;

    // $config[$nametype]['height_photoother'] = 480;

    // $config[$nametype]['width_gallery'] = 480;

    // $config[$nametype]['height_gallery'] = 480;

    // $config[$nametype]['ratio'] = 2;

    // $config[$nametype]['amount_images'] = 1;

    // $config[$nametype]['img_type'] = '.jpg|.gif|.png|.jpeg|.gif|.JPG|.PNG|.JPEG|.Png|.GIF';

    // $config[$nametype]['img_type_photo'] = '.jpg|.gif|.png|.jpeg|.gif|.JPG|.PNG|.JPEG|.Png|.GIF';

    // $config[$nametype]['file_type'] = 'pdf|PDF';

    // $config[$nametype]['file_type'] = 'doc|docx|pdf|rar|zip|ppt|pptx|DOC|DOCX|PDF|RAR|ZIP|PPT|PPTX|xls|jpg|png|gif|JPG|PNG|GIF|xls|XLS';



    /* Footer */

    $nametype = "lienhe";

    $config[$nametype]['com'] = '';

    $config[$nametype]['menu'] = false;

    $config[$nametype]['title_main'] = "Thông tin liên hệ";

    $config[$nametype]['images'] = true;

    $config[$nametype]['images2'] = false;

    $config[$nametype]['file'] = false;

    $config[$nametype]['tieude'] = true;

    $config[$nametype]['mota'] = false;

    $config[$nametype]['mota_cke'] = false;

    $config[$nametype]['noidung'] = true;

    $config[$nametype]['noidung_cke'] = false;

    $config[$nametype]['mota_other'] = false;

    $config[$nametype]['slogan'] = false;

    $config[$nametype]['giatri'] = false;

    $config[$nametype]['video'] = false;

    $config[$nametype]['seo'] = true;

    $config[$nametype]['gallery'] = false;

    $config[$nametype]['width'] = 517;

    $config[$nametype]['height'] = 502;

    $config[$nametype]['width2'] = 374;

    $config[$nametype]['height2'] = 549;

    $config[$nametype]['banner'] = false;

    $config[$nametype]['width_banner'] = 1440;

    $config[$nametype]['height_banner'] = 280;

    $config[$nametype]['width_poster'] = 514;

    $config[$nametype]['height_poster'] = 329;

    $config[$nametype]['width_photoother'] = 480;

    $config[$nametype]['height_photoother'] = 480;

    $config[$nametype]['width_gallery'] = 480;

    $config[$nametype]['height_gallery'] = 480;

    $config[$nametype]['ratio'] = 2;

    $config[$nametype]['amount_images'] = 1;

    $config[$nametype]['img_type'] = '.jpg|.gif|.png|.jpeg|.gif|.JPG|.PNG|.JPEG|.Png|.GIF';

    $config[$nametype]['img_type_photo'] = '.jpg|.gif|.png|.jpeg|.gif|.JPG|.PNG|.JPEG|.Png|.GIF';

    $config[$nametype]['file_type'] = 'doc|docx|pdf|rar|zip|ppt|pptx|DOC|DOCX|PDF|RAR|ZIP|PPT|PPTX|xls|jpg|png|gif|JPG|PNG|GIF|xls|XLS';


    /* Footer */

    // $nametype = "footer";

    // $config[$nametype]['com'] = '';

    // $config[$nametype]['menu'] = false;

    // $config[$nametype]['title_main'] = "Thông tin footer";

    // $config[$nametype]['images'] = false;

    // $config[$nametype]['images2'] = false;

    // $config[$nametype]['file'] = false;

    // $config[$nametype]['tieude'] = false;

    // $config[$nametype]['mota'] = false;

    // $config[$nametype]['mota_cke'] = false;

    // $config[$nametype]['noidung'] = true;

    // $config[$nametype]['noidung_cke'] = true;

    // $config[$nametype]['mota_other'] = false;

    // $config[$nametype]['slogan'] = false;

    // $config[$nametype]['giatri'] = false;

    // $config[$nametype]['video'] = false;

    // $config[$nametype]['seo'] = false;

    // $config[$nametype]['gallery'] = false;

    // $config[$nametype]['width'] = 517;

    // $config[$nametype]['height'] = 502;

    // $config[$nametype]['width2'] = 374;

    // $config[$nametype]['height2'] = 549;

    // $config[$nametype]['banner'] = false;

    // $config[$nametype]['width_banner'] = 1440;

    // $config[$nametype]['height_banner'] = 280;

    // $config[$nametype]['width_poster'] = 514;

    // $config[$nametype]['height_poster'] = 329;

    // $config[$nametype]['width_photoother'] = 480;

    // $config[$nametype]['height_photoother'] = 480;

    // $config[$nametype]['width_gallery'] = 480;

    // $config[$nametype]['height_gallery'] = 480;

    // $config[$nametype]['ratio'] = 2;

    // $config[$nametype]['amount_images'] = 1;

    // $config[$nametype]['img_type'] = '.jpg|.gif|.png|.jpeg|.gif|.JPG|.PNG|.JPEG|.Png|.GIF';

    // $config[$nametype]['img_type_photo'] = '.jpg|.gif|.png|.jpeg|.gif|.JPG|.PNG|.JPEG|.Png|.GIF';

    // $config[$nametype]['file_type'] = 'doc|docx|pdf|rar|zip|ppt|pptx|DOC|DOCX|PDF|RAR|ZIP|PPT|PPTX|xls|jpg|png|gif|JPG|PNG|GIF|xls|XLS';



    return $config;

